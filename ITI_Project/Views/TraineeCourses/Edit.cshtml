
@model Trainee_Course
@{
    SelectList Crs = new SelectList(ViewBag.Crs, "CourseId", "Name"); //Id, name are property of courses which in list "crs"
    SelectList Degrs = new SelectList(ViewBag.degrees);
}
<form action="/TraineeCourses/EditSave">
    <input type="hidden" name="TraineeId" value="@Model?.TraineeId" />

    <div class="form-group">
        <label for="exampleInputCourseName">Course Name</label>
        @* <input type="text" class="form-control" value="@Model?.CourseId" name="CourseId"> *@ @*Pure Html*@
        <select class="form-control" asp-for="CourseId" asp-items="Crs">
            @*Tag html*@

        </select>
    </div>
    <div class="form-group">
        <label for="exampleInputage">degree</label>

        <select class="form-control" asp-for="degree" asp-items="Degrs">
            @*Tag html*@

        </select>
    </div>
    <br />
    <br />
    <button type="submit" class="btn btn-primary">Save</button>
</form>
